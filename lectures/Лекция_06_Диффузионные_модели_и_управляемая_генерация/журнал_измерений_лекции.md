# Журнал измерений к лекции 6 (20.09.2026)

Числа лекции 6 получены тремя способами: (1) арифметика по формулам расписания шума (`fig_forward.py`, numpy, без обучения); (2) демонстрационная диффузионная модель на плоскости `lec06_demo.py`, обученная и выполненная в среде Engee 26.8.2-H3 на процессоре (Python 3.11, torch 2.4.1+cu121, 4 потока), результаты — `demo6.json` (значения переписаны с экрана Engee, файл в Engee: `/user/ii-in-kt/demo6.json`); (3) замер SD Turbo в Engee (`sdturbo_bench.py`, журнал `журнал_замера_SD_Turbo_Engee.md`) и результаты ЛР 5–6 в Google Colab (Tesla T4, 19.09.2026, лекция 4). Демонстрация и замер выполнены один раз; для демонстрации — три обученные модели (seed 101–103) и три seed сэмплирования, значения — среднее по моделям и разброс $s$ между моделями.

| Утверждение лекции | Источник | Значение |
|---|---|---|
| Расписание: $\bar\alpha_t$, SNR при $t$ = 1; 50; 100; 250; 500; 750; 1000 | арифметика (линейное расписание, $T=1000$, $\beta$ от $10^{-4}$ до 0,02), таблица в `demo6.json` | 0,9999; 0,971; 0,897; 0,524; 0,0786; 0,00335; 0,00004; SNR 40,0; 15,3; 9,4; 0,4; −10,7; −24,7; −43,9 дБ |
| Сигнала и шума поровну; ниже 1 % сигнала | арифметика, `fig_forward.py` | $t=260$; $t=674$; на последнем шаге 0,004 % |
| PSNR между $x_t$ и $x_0$, сцена «диск» (ЛР 7), шум seed 101 | арифметика, `fig_forward.py` | 40,0; 27,2; 16,2; 11,9; 8,0; 7,2; 7,2 дБ при $t$ = 1; 10; 50; 100; 250; 500; 750 |
| Чистота настоящих данных; разброс; пол шума $E$ | демо | 0,984; 0,696; −0,0006 |
| Время обучения одной модели | демо, Engee | 72,2; 68,2; 73,5 с |
| Ошибка оценки $\hat x_0$ (RMSE, 4000 точек) при $t$ = 10; 100; 250; 500; 750; 990 | демо | сеть 0,065; 0,413; 0,677; 0,769; 0,796; 3,54; «не убирать шум» 0,066; 0,489; 1,361; 4,876; 24,23; 202,6; «центр класса» 0,779 |
| $E$ по числу шагов DDIM: 1; 2; 3; 5; 10; 25; 50; 100 | демо | 2,343; 2,322; 0,068; 0,026; 0,006; 0,0006; −0,0002; −0,0005 |
| Время сэмплирования 3000 точек DDIM: 1…100 шагов | демо | 0,016; 0,030; 0,040; 0,065; 0,125; 0,316; 0,716; 1,374 с |
| DDPM, 1000 шагов | демо | $E=-0{,}0006$; чистота 0,985; 14,28 с |
| Воспроизводимость DDIM при одном шуме | демо | максимальное расхождение 0,0 (три модели) |
| Безклассификаторное управление, $s$ = 1; 2; 3; 5; 8 | демо (50 шагов DDIM) | чистота 0,986; 1,000; 1,000; 1,000; 1,000; разброс 0,649; 0,638; 0,856; 1,391; 2,343; $E$ −0,0002; 0,063; 0,165; 0,483; 1,162 |
| Время SD Turbo, 512×512, 1; 2; 4 шага, Engee | замер, повтор 1 | 18,0; 22,8; 34,2 с; прямая ≈ 12,3 + 5,4·n (МНК по трём точкам) |
| Время SD Turbo при 1 шаге: 256; 384; 512 | замер, повтор 1 | 4,8; 9,8; 18,0 с (73; 66; 69 с на мегапиксель) |
| SD Turbo в Colab, 1 шаг 512×512 | ЛР 6 (лекция 4) | 0,34 с; отношение Engee/Colab ≈ 53 |
| Пик памяти, загрузка модели (Engee) | замер | 7144 МБ (`ru_maxrss`); 64,1 с (первый запуск, с загрузкой весов) |
| LoRA для 1024×1024, $r=8$ | арифметика | 16 384 вместо 1 048 576 (1,6 %) |

**Иллюстрации.** `cmp_toy.png` — лист `fig_toy.png` из Engee (данные и выборки при $s$ = 1, 3, 8; модель seed 101, шум seed 101), снят с экрана Engee; подписи панелей нарисованы кодом, потому что верхние строки исходного снимка обрезаны. Остальные рисунки построены `figs6.py` по значениям таблицы; сцена «диск» на прямом процессе нарисована по формулам.

**Ограничения.** Повторный запуск демонстрации не выполнялся (один запуск на среду); побитовая воспроизводимость обучения не проверялась. Причина плохого качества при 1–2 шагах DDIM и рост ошибки $\hat x_0$ при $t=990$ отдельным опытом не изолированы. Экстраполяция времени SD Turbo вне 1–4 шагов — оценка по прямой. Значения демо переписаны с экрана Engee вручную; файл `demo6_log.txt` (журнал обучения, потери отдельных пакетов 0,17–0,22 после первых 2000 итераций) в Engee сохранён, но в эту папку не копировался.

