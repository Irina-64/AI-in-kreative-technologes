# Журнал выполнения демонстрационного варианта ЛР 3 в Engee

Дата: 19.09.2026. Среда: Engee 26.8.2-H3 (заголовок вкладки браузера), личный кабинет 26.9.1. Язык: Julia 1.12.4 (`VERSION` → `v"1.12.4"`).
Способ выполнения: тексты функций `lab03_lib.jl` введены в командную строку Engee без комментариев; результаты считаны из вывода командной строки и панели «Переменные».

## Проверка доступности пакетов (`Base.find_package`)
Images — есть; FileIO — есть; TestImages — есть; Plots — есть; CairoMakie — есть; Statistics — есть; FFTW — есть; ImageFiltering, Colors, ImageBase, ImageIO, PNGFiles как отдельные зависимости проекта — нет (операции берутся из `Images`).
После `using Images` определены: imresize, imfilter, erode, dilate, imgradients, channelview, colorview, adjust_histogram, otsu_threshold, assess_psnr, mapwindow, padarray, imrotate, Kernel, build_histogram, clamp01nan.

## Проверка ввода-вывода
`save` и `load` PNG во временный каталог: файл создан, размер (16, 16), 413 байт; рабочий каталог `pwd()` = `/user`.
Построение графика: `using Plots; bar(1:32, hist_counts(g, 32))` и `savefig` — выполнены, файл создан.

## Демонстрационные значения
```text
describe_image(make_scene(101)):
  size = (128, 128), eltype = "RGB{Float64}", min = 0.08115622264322489, max = 0.9185814096363565, bytes = 393216
maxdiff luma (вручную и Gray) = 9.856187532797378e-9
sha256(яркость, seed 101)[1:16] = 50567c6b9e5e4ac1   (повторное построение сцены: тот же результат)
variant_summary(1): psnr = 34.594244830067446
  mean:     base 0.471941890422909,  s 8.38856208237571e-5,   after 0.47080973307291635, d -0.0011321573499926685, ratio 13.496, значимо
  contrast: base 0.06424645499928484, s 0.00015397990431180933, after 0.06819340057161277, d 0.003946945572327928,  ratio 25.633, значимо
  entropy:  base 2.687327799345528,  s 0.004443699253370972, after 1.78211699775138,   d -0.9052108015941478,     ratio 203.707, значимо
```

## Проверка всех 30 вариантов
```text
N=30  DEG=Int64[]  SIGDIST(0..3)=[5, 0, 6, 19]  PSNR=8.3..58.9
```
Вырожденных вариантов нет. Результаты по отдельным вариантам в материалы не включены.

## Повторное выполнение: файл целиком через include (19.09.2026)
Файл `lab03_lib.jl` загружен пользователем в `/user/`; сверка: локальный и загруженный файлы совпали — размер 5027 байт, SHA-256 `cd1622643e7cfe7cc01bb7d8067e4802a76f6a76e9a6833ba4c09c84bb9fa62a`.
`@time include("lab03_lib.jl")` — выполнено без ошибок (0,042 с при уже загруженных пакетах); определены `make_scene` и `variant_summary`.
Пересчитаны после перезапуска сессии среды и совпали с первым прогоном до последнего знака: `describe_image(make_scene(101))`, максимум модуля разности двух способов получения яркости, контрольная сумма `50567c6b9e5e4ac1` (дважды), `variant_summary(1)` (PSNR 34.594244830067446; строки mean, contrast, entropy), а также сводка 30 вариантов: `N=30  DEG=Int64[]  SIGDIST(0..3)=[5, 0, 6, 19]  PSNR=8.3..58.9`.