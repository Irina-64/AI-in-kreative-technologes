# Журнал выполнения ЛР 9, часть 1 (Engee, 20.09.2026)

**Среда.** Engee 26.8.2-H3 (страница аккаунта показывала версию 26.9.1 — обновление среды не проверялось), процессор, Python 3.11 (`/user/.user_venvs/py3.11`), torch 2.4.1+cu121, diffusers 0.32.2, transformers 4.57.6. SD Turbo: `stabilityai/sd-turbo`, ревизия `b261bac6fd2cf515557d5d0707481eafa0485ec2`, float32, 4 потока, генератор CPU. CLIP: `openai/clip-vit-large-patch14`, ревизия `32bd64288804d66eefd0ccbe215aa642df71cc41`, `use_safetensors=True`. Параметры генерации: 512×512, 1 шаг, `guidance_scale` = 0,0, seed 101–105. Скрипты — `src/` (`run_lr09.py` запускался в папке `/user/ii-in-kt/lr09/`).

**Ход работы.**

1. Первый запуск в 01:17 (по часам среды) упал при загрузке CLIP ViT-B/32: `ValueError … torch.load … CVE-2025-32434` (transformers 4.57 не загружает pickle-веса при torch < 2.6; в закреплённой ревизии B/32 нет `model.safetensors`, а главная ветка репозитория указывает на ту же ревизию). Принято решение взять CLIP ViT-L/14 (есть `model.safetensors`); константы и комментарий добавлены в `lab09_lib.py`. Абсолютные значения CLIP несопоставимы с ЛР 6.
2. Перезапуск с ViT-L/14: расчёт контекстов 1–4 шёл около 2 минут на вариант (8–10 минут на контекст вместе с режимом A); режим A каждого контекста считается один раз (одинаков для шести факторов).
3. Сеанс Engee останавливался сам дважды (после зависания вкладки браузера; страница переадресовывала на личный кабинет), процесс при этом терялся. Запуск возобновляемый (пропускает готовые варианты): первый обрыв — после варианта 20, второй — после варианта 25; расчёт возобновлён с последнего невыполненного варианта; результаты первых частей не пересчитывались. Изображение варианта 21 записано до обрыва и перезаписано при повторе. Общее время счёта — около 2 часов с перерывами; последний запуск (варианты 26–30) занял 511 с (с загрузкой моделей).
4. Сводка `summary9.txt` построена `summarize9.py` и прочитана с экрана консоли (значения переписаны вручную, суммы значимых проверены: clip_target 13, clip_subject 10, edge 13, contrast 13 из 30).
5. Часть 2 (ответы языковой модели): проверка `demo9_llm.py` выполнена и в Engee, и локально; вывод совпал (K1–K4 по трём приёмам и пример ошибки).

**Время генерации.** По среднему времени изображения режима B в вариантах 18–24 с (первый вызов с загрузкой не учитывается).

**Ограничения.** Один запуск каждого варианта; повторного полного запуска нет. Изображения получены в float32 на процессоре и не совпадают побитово с изображениями ЛР 5–6 (float16, видеокарта). Значения в `results_30_summary.txt` округлены (две–четыре значащие цифры по метрике); полные результаты — файл `res9.jsonl` в Engee (`/user/ii-in-kt/lr09/`), в папку курса не копировался.
