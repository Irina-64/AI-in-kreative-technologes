# Журнал выполнения демонстрационного варианта ЛР 5 в Google Colab

Дата: 19.09.2026. Среда: Google Colab, ускоритель «Графический процессор T4» (Tesla T4, 15360 МиБ). Python 3.13.15; torch 2.11.0+cu128; torchvision 0.26.0+cu128; diffusers 0.40.0; transformers 5.16.1; accelerate 1.14.0; safetensors 0.8.0; Pillow 11.3.0. Платформа: Linux-6.6.122+-x86_64-with-glibc2.39. Модель `stabilityai/sd-turbo`, ревизия `b261bac6fd2cf515557d5d0707481eafa0485ec2` (на дату — последняя; проверено через API Hugging Face), файлы fp16, веса без авторизации.

Ноутбук: `lab05.ipynb` (Google Диск проверявшего): ячейка 1 — `%%writefile lab05_lib.py`, ячейка 2 — демонстрационный код, ячейка 3 — проверка 30 вариантов.

## Сеанс 1
Библиотека введена в ячейку; SHA-256 введённого текста совпал с локальным файлом (`4eb176506585ffcda1702605a767309fb451808229c3622f80ce882b7fa2ce1a`, 6447 байт).

Диагностика до основного прогона:
- fp16 и fp32 при одном seed (генератор на CPU): PSNR 11,35 дБ (1 шаг), 8,91 (2), 8,32 (4). Изображения не вырожденные (среднее яркости 88,8 и 77,0).
- Шум `torch.randn` на CPU в float16 и float32 при одном seed отличается: максимум модуля разности 5,30.
- Одинаковый начальный шум (float32, затем приведение): fp16 против fp32 PSNR 41,67 дБ (1 шаг), 33,42 (4 шага).
- Свежая float32-конвейерная загрузка и приведение `pipe.to(float32)` дали побитово совпадающие изображения (PSNR = ∞).

Проверка 30 вариантов (`variant_summary(pipe, n)`, seed = 1000 + n), сводка по условиям: повтор — identical 5 из 5; seed + 1 — different 5; устройство генератора — different 5; тип вычислений — different 5 (при общем шуме PSNR 37,2–50,6 дБ); формат файла — different 5 (PSNR 28,1–37,5 дБ, максимум различия 30–101); пакет — close 5 (PSNR 47,2–51,1 дБ, максимум различия 12–30). Исключений нет. Суммарное время генерации 27,8 с.

## Демонстрационный запуск (ячейка 2), сеанс 1
seed 20260919, бриф 1, 1 шаг, guidance 0,0, 512×512, генератор CPU, float16. Загрузка первый раз 22,1 с (загрузка весов), повторная в том же сеансе 3,0 с; генерация 0,36 с. SHA-256 `ac45a7ba8c514b22c1b4cbe0e5c3cfdec62ee3d076b633aaeeadbca43f8a04e9`. Повтор: совпал. Общий генератор на два вызова: первый совпал, второй нет; PSNR 11,14 дБ.

## Сеанс 2 (новая виртуальная машина)
Среда удалена командой `runtime.unassign()` и создана заново; библиотека записана заново, SHA-256 введённого текста совпал (`4eb17650…`); демонстрационный код — SHA-256 `53b3b13f19e6754bda1cde58ef09f1e45b1595d1c4dab2a9b02bd4d99d731ce4`, 2677 байт. Загрузка модели 27,9 с, генерация 0,27 с. SHA-256 результата **совпал** с сеансом 1. Общий генератор: тот же результат (второй запуск не совпал; PSNR 11,14 дБ).

Проверка 30 вариантов повторена в сеансе 2: сводка по условиям та же (repeat 5 identical; batch 5 close; остальные 5 different); значения PSNR выборочно (варианты 2–6, 16, 20, 23, 29) совпали с сеансом 1 до сотых. Суммарное время генерации 25,2 с.

## Контрольный запуск на CPU (17.09.2026)
Сохранён в `cpu_evidence_2026-09-17`: Linux, torch 2.7.1+cpu, diffusers 0.40.0, 1 поток, прежний код ЛР; загрузка 164 с, генерация 93,7 с; SHA-256 двух запусков совпал. К настоящему демо не относится (другой код, seed и запрос).
